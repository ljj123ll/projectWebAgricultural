# Spring Boot ???????YAML ???????????????Tab?
spring:
  # ??????????????YAML ?????
  application:
    name: agricultural
  # ????????MySQL?????????????application???
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver # MySQL 8.0+ ????
    url: jdbc:mysql://localhost:3306/agricultural_assist?serverTimezone=Asia/Shanghai&characterEncoding=utf8mb4&useSSL=false&allowPublicKeyRetrieval=true
    username: root # MySQL ?????
    password: 798220040520ljj # ??MySQL????????????
  # Redis ????datasource????????
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 3000
      database: 0

# ???????spring????????spring???
server:
  port: 8080 # ??????

# MyBatis ????spring?server??????
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.agricultural.assistplatform.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl